import React from 'react';
import SideBarMenu from '../SideBarMenu';
import Wrapper from '../Wrapper';

const App = () => (
    <Wrapper>
        <SideBarMenu />
    </Wrapper>
);

export default App;
